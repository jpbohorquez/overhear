# Core Audio & UI
PyAudio==0.2.14
sounddevice==0.4.6
numpy==1.24.3
customtkinter==5.2.2
darkdetect==0.8.0

# Transcription
faster-whisper==1.0.3

# Utilities
python-dotenv==1.0.0
requests
toml==0.10.2
# For Windows loopback support
# desktop-magic-capture (optional, but sounddevice/pyaudio usually suffice with WASAPI)
